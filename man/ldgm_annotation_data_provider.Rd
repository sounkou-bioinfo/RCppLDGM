% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interfaces.R
\name{ldgm_annotation_data_provider}
\alias{ldgm_annotation_data_provider}
\title{Create a Callback-Based Annotation Provider}
\usage{
ldgm_annotation_data_provider(frame, annotation_cols, partition = NULL)
}
\arguments{
\item{frame}{Function called as
\verb{frame(annotation_cols = <chr>, required_cols = <chr>)} and expected to
return a data frame.}

\item{annotation_cols}{Annotation columns selected for GraphREML or score-test
work.}

\item{partition}{Optional function called as
\verb{partition(metadata, chrom_col = NULL, pos_col = NULL, required_cols = <chr>, annotation_cols = <chr>)}
and expected to return a list of data frames, one per metadata row.}
}
\value{
An object implementing \link{LdgmAnnotationData}.
}
\description{
Builds an object implementing \link{LdgmAnnotationData} from callback functions
rather than an eagerly materialized data frame. This lets database-backed or
otherwise lazy annotation tables expose only the interface contract needed by
the current workflow.
}
