% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bricks.R
\name{ldgm_brick_edges_from_tables}
\alias{ldgm_brick_edges_from_tables}
\title{Brick Tree-Sequence Edge Tables from Canonical Tree-Diff Tables}
\usage{
ldgm_brick_edges_from_tables(
  initial_edges,
  transitions,
  edges_out,
  edges_in,
  node_state,
  num_samples,
  recombination_freq_threshold = NULL
)
}
\arguments{
\item{initial_edges}{Data frame of edges active in the first marginal tree
with columns \code{left}, \code{right}, \code{parent}, and \code{child}.}

\item{transitions}{Data frame with one row per subsequent tree-diff interval
and columns \code{transition} and \code{left}, where \code{left} is the breakpoint at the
start of the new interval.}

\item{edges_out}{Data frame with columns \code{transition} and \code{child}, describing
edge children removed at each transition.}

\item{edges_in}{Data frame with columns \code{transition}, \code{left}, \code{right},
\code{parent}, and \code{child}, describing edge rows added at each transition.}

\item{node_state}{Data frame with columns \code{transition}, \code{node},
\code{prev_parent}, \code{curr_parent}, \code{time}, and \code{curr_num_samples}. Rows describe
node state in the previous/current tree pair for each transition.}

\item{num_samples}{Number of samples in the tree sequence.}

\item{recombination_freq_threshold}{Minimum child frequency above which
recombination-induced edge splits are made. \code{NULL} follows upstream default
behavior and is treated as zero.}
}
\value{
Data frame with columns \code{left}, \code{right}, \code{parent}, and \code{child}.
}
\description{
Table-oriented Rcpp port of upstream \code{ldgm.brick_ts()} edge splitting. This
function does not yet load or own a tree-sequence object; instead it accepts
canonical tree-diff tables extracted from a tree sequence and returns the
bricked edge table. It is the native bricking kernel that a future \code{.trees} /
tskit adapter can feed directly.
}
