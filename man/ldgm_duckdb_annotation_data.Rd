% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb-providers.R
\name{ldgm_duckdb_annotation_data}
\alias{ldgm_duckdb_annotation_data}
\title{Create a DuckDB-backed Annotation Provider}
\usage{
ldgm_duckdb_annotation_data(
  con,
  from,
  annotation_cols,
  chrom_col = "CHR",
  pos_col = "POS"
)
}
\arguments{
\item{con}{A live DuckDB connection created with \code{\link[DBI:dbConnect]{DBI::dbConnect()}} and
\code{\link[duckdb:duckdb]{duckdb::duckdb()}}.}

\item{from}{Annotation source. Supply a scalar table/view name, a \code{DBI::Id()}
for schema-qualified objects, or \code{DBI::SQL()} containing a valid DuckDB
\code{FROM} source such as \verb{(SELECT ...)}.}

\item{annotation_cols}{Annotation columns selected for GraphREML or
score-test work.}

\item{chrom_col}{Chromosome column in \code{from}.}

\item{pos_col}{Position column in \code{from}.}
}
\value{
An object implementing \link{LdgmAnnotationData}.
}
\description{
Builds a callback-based \link{LdgmAnnotationData} implementation over a DuckDB
table, view, or subquery. The provider only projects requested annotation and
metadata columns into R, and partitions by LDGM metadata blocks inside
DuckDB.
}
\examples{
\dontshow{if (requireNamespace("DBI", quietly = TRUE) && requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
con <- DBI::dbConnect(duckdb::duckdb(), dbdir = ":memory:")
on.exit(DBI::dbDisconnect(con, shutdown = TRUE), add = TRUE)
DBI::dbWriteTable(
  con,
  "annotations",
  data.frame(
    CHR = 1L,
    POS = c(10L, 20L),
    af = c(0.4, 0.2)
  )
)
provider <- ldgm_duckdb_annotation_data(con, "annotations", annotation_cols = "af")
ldgm_annotation_data_frame(provider)
\dontshow{\}) # examplesIf}
}
