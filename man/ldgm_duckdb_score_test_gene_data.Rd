% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb-providers.R
\name{ldgm_duckdb_score_test_gene_data}
\alias{ldgm_duckdb_score_test_gene_data}
\title{Create a DuckDB-backed Gene Score-Test Row-Data Provider}
\usage{
ldgm_duckdb_score_test_gene_data(
  con,
  from,
  chrom_col = "CHR",
  pos_col = "POS",
  gene_id_col = "gene_id",
  gene_name_col = "gene_name",
  jackknife_col = NULL
)
}
\arguments{
\item{con}{A live DuckDB connection created with \code{\link[DBI:dbConnect]{DBI::dbConnect()}} and
\code{\link[duckdb:duckdb]{duckdb::duckdb()}}.}

\item{from}{Gene row-data source. Supply a scalar table/view name, a
\code{DBI::Id()} for schema-qualified objects, or \code{DBI::SQL()} containing a
valid DuckDB \code{FROM} source such as \verb{(SELECT ...)}.}

\item{chrom_col}{Source chromosome column mapped to HDF5 \code{CHR}.}

\item{pos_col}{Source position column mapped to HDF5 \code{POS}.}

\item{gene_id_col}{Source column mapped to HDF5 \code{gene_id}.}

\item{gene_name_col}{Source column mapped to HDF5 \code{gene_name}.}

\item{jackknife_col}{Optional source column mapped to HDF5
\code{jackknife_blocks}.}
}
\value{
An object implementing \link{LdgmScoreTestGeneData}.
}
\description{
Builds a callback-based \link{LdgmScoreTestGeneData} implementation over a DuckDB
table, view, or subquery. Requested columns are projected into R with the
canonical HDF5 names expected by the gene score writer.
}
\examples{
\dontshow{if (requireNamespace("DBI", quietly = TRUE) && requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
con <- DBI::dbConnect(duckdb::duckdb(), dbdir = ":memory:")
on.exit(DBI::dbDisconnect(con, shutdown = TRUE), add = TRUE)
DBI::dbWriteTable(
  con,
  "gene_rows",
  data.frame(
    chrom = c(1L, 1L),
    position = c(100L, 250L),
    ensg = c("ENSG1", "ENSG2"),
    symbol = c("GENE1", "GENE2")
  )
)
provider <- ldgm_duckdb_score_test_gene_data(
  con,
  "gene_rows",
  chrom_col = "chrom",
  pos_col = "position",
  gene_id_col = "ensg",
  gene_name_col = "symbol"
)
ldgm_score_test_gene_data_frame(provider)
\dontshow{\}) # examplesIf}
}
