% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duckdb-providers.R
\name{ldgm_duckdb_summary_stats}
\alias{ldgm_duckdb_summary_stats}
\title{Create a DuckDB-backed Summary-Statistics Provider}
\usage{
ldgm_duckdb_summary_stats(
  con,
  from,
  required_cols = c("SNP", "Z"),
  chrom_col = "CHR",
  pos_col = "POS"
)
}
\arguments{
\item{con}{A live DuckDB connection created with \code{\link[DBI:dbConnect]{DBI::dbConnect()}} and
\code{\link[duckdb:duckdb]{duckdb::duckdb()}}.}

\item{from}{Summary-statistics source. Supply a scalar table/view name, a
\code{DBI::Id()} for schema-qualified objects, or \code{DBI::SQL()} containing a
valid DuckDB \code{FROM} source such as \verb{(SELECT ...)}.}

\item{required_cols}{Default summary-statistics columns requested when
callers do not supply a narrower projection.}

\item{chrom_col}{Chromosome column in \code{from}.}

\item{pos_col}{Position column in \code{from}.}
}
\value{
An object implementing \link{LdgmSummaryStats}.
}
\description{
Builds a callback-based \link{LdgmSummaryStats} implementation over a DuckDB
table, view, or subquery. The provider only projects requested columns into
R. When callers partition by LDGM metadata blocks, the interval join runs
inside DuckDB against a temporary block table instead of materializing the
full source table first. For BLUP or clumping workflows, include \code{REF} and
\code{ALT} in the DuckDB source when you want allele-aware phase matching.
}
\examples{
\dontshow{if (requireNamespace("DBI", quietly = TRUE) && requireNamespace("duckdb", quietly = TRUE)) withAutoprint(\{ # examplesIf}
con <- DBI::dbConnect(duckdb::duckdb(), dbdir = ":memory:")
on.exit(DBI::dbDisconnect(con, shutdown = TRUE), add = TRUE)
DBI::dbWriteTable(
  con,
  "sumstats",
  data.frame(
    CHR = 1L,
    POS = c(10L, 20L),
    SNP = c("rs1", "rs2"),
    REF = c("A", "C"),
    ALT = c("G", "T"),
    Z = c(0.5, -0.25)
  )
)
provider <- ldgm_duckdb_summary_stats(con, "sumstats")
ldgm_summary_stats_frame(provider)
\dontshow{\}) # examplesIf}
}
