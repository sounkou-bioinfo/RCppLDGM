% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/precision.R
\name{ldgm_inverse_diagonal}
\alias{ldgm_inverse_diagonal}
\title{Diagonal of the Inverse Precision Matrix}
\usage{
ldgm_inverse_diagonal(
  precision,
  method = c("exact", "xdiag", "hutchinson"),
  n_samples = 100L,
  seed = NULL,
  probes = NULL,
  initialization = NULL,
  return_initialization = FALSE,
  ...
)
}
\arguments{
\item{precision}{Sparse precision matrix or \code{ldgm_precision} object.}

\item{method}{One of \code{"exact"}, \code{"hutchinson"}, or \code{"xdiag"}.}

\item{n_samples}{Number of random Rademacher probe vectors for stochastic
methods. The actual count is \code{min(n, n_samples)}.}

\item{seed}{Optional random seed for probe generation.}

\item{probes}{Optional numeric probe matrix for deterministic tests or reuse.}

\item{initialization}{Optional GraphLD-style initialization: a two-element
list whose first element is the probe matrix and whose second element is a
previous solved-probe matrix. The current direct-solve implementation
accepts the second element for API compatibility and recomputes the solve.}

\item{return_initialization}{If \code{TRUE}, return a list containing the diagonal
estimate and solved probe matrix for later reuse.}

\item{...}{Reserved for future estimators.}
}
\value{
Numeric vector containing \code{diag(solve(precision))}, or a list when
\code{return_initialization = TRUE} or \code{initialization} is supplied.
}
\description{
Computes diagonal entries of the inverse of a full precision matrix or a
selected Schur-complement view. \code{method = "exact"} uses dense inversion for
conformance fixtures and small blocks. \code{"hutchinson"} and \code{"xdiag"} provide
stochastic estimators aligned with GraphLD's \code{PrecisionOperator} API.
}
