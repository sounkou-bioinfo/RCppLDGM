% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bricks.R
\name{ldgm_make_ldgm_from_tree_tables}
\alias{ldgm_make_ldgm_from_tree_tables}
\title{Make an LDGM from Canonical Tree-Diff Tables}
\usage{
ldgm_make_ldgm_from_tree_tables(
  initial_edges,
  transitions,
  edges_out,
  edges_in,
  node_state,
  sample_nodes,
  mutations,
  path_threshold,
  recombination_freq_threshold = NULL,
  return_intermediates = FALSE
)
}
\arguments{
\item{initial_edges, transitions, edges_out, edges_in, node_state}{See
\code{\link[=ldgm_brick_edges_from_tables]{ldgm_brick_edges_from_tables()}}.}

\item{sample_nodes}{Integer vector of sample node ids.}

\item{mutations}{Data frame with mutation id, position, and node columns. If
columns \code{ancestral_state}, \code{derived_state}, and optionally \code{site} are
present, an upstream-compatible SNP-list is also returned.}

\item{path_threshold}{Maximum path weight retained by LDGM reduction.}

\item{recombination_freq_threshold}{Minimum frequency for recombination edge
splitting; \code{NULL} follows upstream default behavior.}

\item{return_intermediates}{Whether to return bricked edges, brick/event
tables, and mutation mapping in addition to graph/SNP-list outputs.}
}
\value{
A list containing at least \code{graph}. If allele columns are present in
\code{mutations}, the list also contains \code{snplist}. With
\code{return_intermediates = TRUE}, bricked and derived intermediate tables are
included.
}
\description{
High-level table API for the currently ported LDGM pipeline. This function
starts from canonical tree-diff tables, runs the native bricking kernel,
derives brick/event inputs and mutation mappings, and returns the final LDGM
edge list. It is the table-backed equivalent of upstream \code{ldgm.make_ldgm()};
a future \code{.trees} adapter can feed these same inputs directly.
}
