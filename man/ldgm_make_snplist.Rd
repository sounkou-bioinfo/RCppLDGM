% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snplist.R
\name{ldgm_make_snplist}
\alias{ldgm_make_snplist}
\title{Make an LDGM SNP List from Canonical Tables}
\usage{
ldgm_make_snplist(
  bricks_to_muts,
  sites,
  mutations,
  site_metadata_id = NULL,
  population_frequencies = NULL
)
}
\arguments{
\item{bricks_to_muts}{Named list of zero-based brick ids to mutation ids, or a
data frame with \code{brick} plus either \code{mutation} or semicolon-separated
\code{mutations} columns.}

\item{sites}{Data frame with one row per site and an \code{ancestral_state} column.}

\item{mutations}{Data frame with one row per mutation and a \code{derived_state}
column. If an \code{id} column is present it is used as the zero-based mutation
id; otherwise row order is used.}

\item{site_metadata_id}{Optional metadata key/column to use for a \code{site_ids}
column. If \code{sites} has a column with this name it is used directly;
otherwise a \code{metadata} column containing simple JSON objects is searched.}

\item{population_frequencies}{Optional data frame or named list of numeric
allele-frequency vectors to append to the result.}
}
\value{
A data frame containing \code{index}, \code{anc_alleles}, \code{deriv_alleles}, and
optional \code{site_ids} / population-frequency columns.
}
\description{
Table-oriented R port of upstream \code{ldgm.make_snplist()}. This helper avoids a
hard dependency on a tree-sequence runtime by accepting the upstream-derived
brick-to-mutation mapping plus site and mutation tables. The full \code{brick_ts()}
wiring can feed these canonical inputs once the tree-sequence layer is ported.
}
