% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reml.R
\name{ldgm_prepare_reml_inputs}
\alias{ldgm_prepare_reml_inputs}
\title{Prepare GraphREML Inputs from GraphLD-Style Tables}
\usage{
ldgm_prepare_reml_inputs(
  ldgms,
  sumstats,
  annotation_data,
  metadata = NULL,
  ldgm_dir = NULL,
  population = "EUR",
  populations = NULL,
  chromosomes = NULL,
  sample_size = NULL,
  match_by_position = FALSE,
  z_col = "Z",
  variant_id_col = "SNP",
  ref_allele_col = "REF",
  alt_allele_col = "ALT",
  pos_col = "POS",
  chrom_col = NULL,
  use_surrogate_markers = TRUE
)
}
\arguments{
\item{ldgms}{An \code{ldgm_precision} block, a list of such blocks, a
\link{LdgmBlockCatalog} object, or a path to a GraphLD metadata CSV.}

\item{sumstats}{Summary-statistics data frame or object implementing
\link{LdgmSummaryStats}.}

\item{annotation_data}{Annotation data frame or object implementing
\link{LdgmAnnotationData}.}

\item{metadata}{Optional metadata data frame used to partition \code{sumstats}
when \code{ldgms} is already loaded.}

\item{ldgm_dir}{Directory containing metadata-referenced \code{.edgelist} and
\code{.snplist} files. Defaults to the metadata file directory.}

\item{population}{Population column/name passed to \code{\link[=ldgm_load_ldgm]{ldgm_load_ldgm()}}.}

\item{populations}{Optional population filter for metadata files.}

\item{chromosomes}{Optional chromosome filter for metadata files.}

\item{sample_size}{Optional GWAS sample size override. When \code{NULL}, the mean
\code{N} column from \code{sumstats} is used if available.}

\item{match_by_position}{Match summary statistics by position instead of SNP
identifier.}

\item{z_col}{Summary-statistics Z-score column.}

\item{variant_id_col}{Summary-statistics variant id column.}

\item{ref_allele_col}{Summary-statistics reference allele column.}

\item{alt_allele_col}{Summary-statistics alternate allele column.}

\item{pos_col}{Preferred position column in \code{sumstats} and \code{annotation_data}.}

\item{chrom_col}{Preferred chromosome column in \code{sumstats} and
\code{annotation_data}.}

\item{use_surrogate_markers}{If \code{TRUE}, keep annotation rows without matched
Z scores so \code{\link[=ldgm_run_reml]{ldgm_run_reml()}} can resolve them through surrogate markers.}
}
\value{
A list with class \code{ldgm_reml_inputs} containing low-level
\code{ldgms}/\code{z}/\code{annotations} blocks, inferred \code{sample_size}, \code{annotation_names},
\code{block_names}, full-row output annotation blocks, jackknife annotation
blocks, and per-block row mappings back to the partitioned GraphLD-style
tables.
}
\description{
Converts GraphLD-style summary-statistics, annotation, and block-catalog
inputs into the explicit block lists consumed by \code{\link[=ldgm_run_reml]{ldgm_run_reml()}}. This is a
staging helper for metadata-driven or provider-backed GraphREML workflows:
it keeps the low-level optimizer boundary explicit while giving callers a
single place to resolve block catalogs, merge annotations, preserve empty
metadata blocks, and retain per-block row mappings for output summaries.
}
