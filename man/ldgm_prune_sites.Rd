% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tskit-adapter.R
\name{ldgm_prune_sites}
\alias{ldgm_prune_sites}
\title{Prune Low-Frequency Sites from tskit Tree Sequences}
\usage{
ldgm_prune_sites(
  x,
  threshold,
  python = NULL,
  backend = c("auto", "native", "reticulate")
)
}
\arguments{
\item{x}{A \code{.trees} file path, a native \code{ldgm_tskit_treeseq} handle, or a
Python \code{tskit.TreeSequence} object from \code{reticulate}.}

\item{threshold}{Finite site-frequency threshold in \verb{[0, 0.5]}.}

\item{python}{Optional path to the Python executable containing \code{tskit}; used
only for Python-object inputs.}

\item{backend}{Backend selector. \code{"auto"} and \code{"native"} use vendored tskit C
for \code{.trees} paths and native handles. Python objects require \code{"auto"} or
\code{"reticulate"}.}
}
\value{
A pruned \code{ldgm_tskit_treeseq} handle for native inputs, or a pruned
Python \code{tskit.TreeSequence} object for Python inputs.
}
\description{
Removes sites whose derived-allele frequency is below \code{threshold} or above
\code{1 - threshold}, matching upstream \code{ldgm.utility.prune_sites()} semantics for
tree sequences with exactly one mutation per site. Native \code{.trees} paths and
\code{ldgm_tskit_treeseq()} handles use the vendored tskit C API. Live Python
\code{tskit.TreeSequence} objects use a small reticulate helper that mirrors the
upstream Python loop.
}
