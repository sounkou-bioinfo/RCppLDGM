% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sumstats-readers.R
\name{ldgm_read_parquet_sumstats}
\alias{ldgm_read_parquet_sumstats}
\title{Read One Trait from a Parquet Summary-Statistics File}
\usage{
ldgm_read_parquet_sumstats(file, trait = NULL, maximum_missingness = 1)
}
\arguments{
\item{file}{Path to a Parquet summary-statistics file.}

\item{trait}{Trait to extract. Defaults to the first trait found in the
schema.}

\item{maximum_missingness}{Maximum missingness fraction allowed when an \code{N}
column is present. Defaults to \code{1.0} (no filtering), matching GraphLD.}
}
\value{
A data frame with standardized summary-statistics columns.
}
\description{
R-native adapter for GraphLD's \code{read_parquet_sumstats()}. It extracts a
single trait from \verb{\{trait\}_BETA} and \verb{\{trait\}_SE} columns, standardizes common
variant columns to \code{SNP}, \code{CHR}, \code{POS}, \code{REF}, \code{ALT}, \code{N}, and computes \code{Z}.
Requires optional Parquet support from either \code{nanoparquet} or \code{arrow}.
}
