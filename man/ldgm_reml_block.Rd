% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reml.R
\name{ldgm_reml_block}
\alias{ldgm_reml_block}
\title{Compute One GraphREML Block Likelihood Slice}
\usage{
ldgm_reml_block(
  precision,
  z,
  annotations,
  params,
  sample_size,
  intercept = 1,
  link_fn_denominator = 6e+06,
  diagonal_method = "xdiag",
  n_samples = 100L,
  seed = NULL
)
}
\arguments{
\item{precision}{Sparse precision matrix or \code{ldgm_precision} block.}

\item{z}{Numeric Z-score vector for the block.}

\item{annotations}{Numeric annotation matrix with one row per variant. When
\code{precision} is an \code{ldgm_precision} object, duplicated variants are
aggregated by \code{variant_info$index}, matching GraphLD's \code{variant_indices}.}

\item{params}{Numeric parameter vector, one value per annotation column.}

\item{sample_size}{Positive GWAS sample size.}

\item{intercept}{Positive LDSC intercept scaling applied to the LDGM
precision matrix before the heritability diagonal update.}

\item{link_fn_denominator}{Positive denominator for \code{ldgm_reml_link()}.}

\item{diagonal_method}{Inverse-diagonal method passed to
\code{\link[=ldgm_inverse_diagonal]{ldgm_inverse_diagonal()}} for the gradient.}

\item{n_samples}{Number of stochastic probes for stochastic inverse-diagonal
estimators.}

\item{seed}{Optional random seed.}
}
\value{
A list with \code{likelihood}, \code{gradient}, \code{hessian}, \code{per_variant_h2},
\code{diag_update}, \code{p_z}, and \code{model_precision}.
}
\description{
Initial serial R port of the core GraphLD graphREML block calculation. Given
an LDGM precision block, Z scores, variant annotations, and model parameters,
it builds the GraphREML covariance for \code{Pz}, then returns the Gaussian
likelihood, parameter gradient, average-information Hessian approximation, and
per-variant heritability contributions.
}
\details{
This is a narrow core-kernel interface: file loading, surrogate markers,
multiprocessing, HDF5 score-test output, and full CLI behavior remain outside
this function.
}
