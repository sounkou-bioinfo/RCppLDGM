% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{ldgm_remove_node}
\alias{ldgm_remove_node}
\title{Remove a Node from a Directed Weighted LDGM Graph}
\usage{
ldgm_remove_node(graph, node, path_threshold)
}
\arguments{
\item{graph}{A data frame with integer columns \code{from}, \code{to} and numeric column
\code{weight}, or an object created by \code{ldgm_edge_list()}.}

\item{node}{Integer id of the node to remove.}

\item{path_threshold}{Numeric maximum retained path weight.}
}
\value{
A sorted data frame with columns \code{from}, \code{to}, and \code{weight}.
}
\description{
Native port of the upstream Python \code{ldgm.utility.remove_node()} helper. For a
directed graph, every predecessor of \code{node} is connected to every successor of
\code{node}; the new path weight is the sum of the two incident edge weights. If an
edge already exists, the smaller weight is retained. New edges with combined
weight greater than \code{path_threshold} are discarded, and all edges incident to
\code{node} are removed.
}
\details{
Conformance note: Python \code{networkx.DiGraph} stores one edge per \verb{(from, to)}
pair. If duplicate pairs are supplied here, the last pair in the input wins
before node removal, matching sequential \code{DiGraph} edge insertion.
}
