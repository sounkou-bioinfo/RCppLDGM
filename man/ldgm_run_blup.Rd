% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blup.R
\name{ldgm_run_blup}
\alias{ldgm_run_blup}
\title{Compute BLUP Weights Across LDGM Blocks}
\usage{
ldgm_run_blup(
  ldgms,
  sumstats,
  sigmasq,
  sample_size,
  metadata = NULL,
  ldgm_dir = NULL,
  population = "EUR",
  populations = NULL,
  chromosomes = NULL,
  match_by_position = FALSE,
  z_col = "Z",
  variant_id_col = "SNP",
  ref_allele_col = "REF",
  alt_allele_col = "ALT",
  pos_col = "POS",
  chrom_col = NULL,
  num_processes = NULL,
  run_in_serial = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{ldgms}{An \code{ldgm_precision} object, a list of such objects, or a path to
a GraphLD metadata CSV containing \code{name} and \code{snplistName} columns.}

\item{sumstats}{Summary-statistics data frame, or a list of per-block data
frames when \code{ldgms} is a list and \code{metadata} is not supplied.}

\item{sigmasq}{SNP-heritability variance component.}

\item{sample_size}{GWAS sample size.}

\item{metadata}{Optional metadata data frame used to partition \code{sumstats} when
\code{ldgms} is already loaded.}

\item{ldgm_dir}{Directory containing metadata-referenced \code{.edgelist} and
\code{.snplist} files. Defaults to the metadata file directory.}

\item{population}{Population column/name passed to \code{\link[=ldgm_load_ldgm]{ldgm_load_ldgm()}}.}

\item{populations}{Optional population filter for metadata files.}

\item{chromosomes}{Optional chromosome filter for metadata files.}

\item{match_by_position}{Match summary statistics by position instead of SNP
identifier.}

\item{z_col}{Summary-statistics Z-score column.}

\item{variant_id_col}{Summary-statistics variant id column.}

\item{ref_allele_col}{Summary-statistics reference allele column.}

\item{alt_allele_col}{Summary-statistics alternate allele column.}

\item{pos_col}{Preferred position column in \code{sumstats}.}

\item{chrom_col}{Preferred chromosome column in \code{sumstats}.}

\item{num_processes}{Accepted for GraphLD API compatibility; currently ignored.}

\item{run_in_serial}{Accepted for GraphLD API compatibility; serial execution
is always used currently.}

\item{verbose}{Print a short summary.}
}
\value{
A data frame containing the partitioned summary statistics with a
\code{weight} column.
}
\description{
Serial R port of GraphLD's \code{run_blup()} block scheduler. Each block merges
summary statistics with the LDGM SNP list, phase-aligns the \code{z_col}, runs the
single-block BLUP kernel, and returns the input summary statistics with a
\code{weight} column.
}
