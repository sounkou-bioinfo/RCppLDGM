% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reml.R
\name{ldgm_run_reml}
\alias{ldgm_run_reml}
\title{Run a Serial GraphREML-Style Optimizer}
\usage{
ldgm_run_reml(
  ldgms,
  z,
  annotations,
  params = NULL,
  sample_size,
  annotation_names = NULL,
  num_iterations = 10L,
  convergence_tol = 0.001,
  intercept = 1,
  link_fn_denominator = 6e+06,
  diagonal_method = "xdiag",
  n_samples = 100L,
  seed = NULL,
  num_jackknife_blocks = 100L,
  max_step_halving = 12L,
  score_test_hdf5 = NULL,
  score_test_trait_name = "trait",
  score_test_variant_data = NULL,
  score_test_jackknife_blocks = NULL,
  score_test_diagonal_method = diagonal_method,
  score_test_n_samples = 200L,
  score_test_write_hessian = FALSE,
  score_test_project_annotations = TRUE,
  score_test_overwrite = FALSE
)
}
\arguments{
\item{ldgms}{An \code{ldgm_precision}/sparse precision block or a list of blocks.}

\item{z}{Numeric Z-score vector or list of vectors, one per block.}

\item{annotations}{Numeric annotation matrix or list of matrices, one per
block.}

\item{params}{Optional starting parameter vector. Defaults to zeros.}

\item{sample_size}{Positive GWAS sample size.}

\item{annotation_names}{Optional names for annotation parameters. Defaults to
annotation matrix column names or \code{annot1}, \code{annot2}, ...}

\item{num_iterations}{Maximum number of optimization iterations.}

\item{convergence_tol}{Stop when the absolute likelihood change is below this
threshold.}

\item{intercept, link_fn_denominator, diagonal_method, n_samples, seed}{Passed to
\code{ldgm_reml_block()}.}

\item{num_jackknife_blocks}{Maximum number of grouped block jackknife
replicates used for parameter, heritability, and enrichment standard errors.}

\item{max_step_halving}{Maximum number of step halvings for a proposed Newton
step.}

\item{score_test_hdf5}{Optional HDF5 path. When supplied, final per-variant
score-test gradients are written with \code{\link[=ldgm_write_score_test_hdf5]{ldgm_write_score_test_hdf5()}}.}

\item{score_test_trait_name}{Trait group name to use under \verb{/traits} when
\code{score_test_hdf5} is supplied.}

\item{score_test_variant_data}{Data frame, or list of per-block data frames,
with \code{CHR}, \code{POS}, and \code{RSID}/\code{SNP} columns for HDF5 row data.}

\item{score_test_jackknife_blocks}{Optional jackknife assignments for the HDF5
row data.}

\item{score_test_diagonal_method, score_test_n_samples}{Inverse-diagonal
estimator and probe count used for final per-variant score gradients and
optional Hessian/correction vectors.}

\item{score_test_write_hessian}{If \code{TRUE}, also compute and write the
GraphLD-style per-variant Hessian/correction vector to the score-test HDF5
trait group.}

\item{score_test_project_annotations}{If \code{TRUE}, project annotation columns
out of final per-variant score-test gradients before writing, matching
GraphLD's score-test path.}

\item{score_test_overwrite}{If \code{TRUE}, replace an existing HDF5 file.}
}
\value{
A list containing estimated \code{parameters}, jackknife standard errors
and log10 p-values, \code{heritability}, \code{enrichment}, \code{likelihood_history},
convergence diagnostics, \code{score_test_hdf5} write metadata when requested,
and final block derivatives.
}
\description{
Initial serial scheduler for GraphLD-style graphREML over one or more LDGM
blocks. Each iteration sums block likelihoods, gradients, and
average-information Hessians from \code{ldgm_reml_block()}, proposes a Newton step,
and uses step-halving to require non-decreasing likelihood.
}
\details{
This function is intended as the first R-native workflow surface and
conformance target for graphREML kernels. It includes initial GraphLD-style
pseudo-jackknife standard errors and optional score-test HDF5 output, but does
not yet implement GraphLD's multiprocessing manager or surrogate-marker
handling.
}
