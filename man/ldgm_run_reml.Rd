% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reml.R
\name{ldgm_run_reml}
\alias{ldgm_run_reml}
\title{Run a Serial GraphREML-Style Optimizer}
\usage{
ldgm_run_reml(
  ldgms,
  z = NULL,
  annotations = NULL,
  params = NULL,
  sample_size = NULL,
  annotation_names = NULL,
  num_iterations = 10L,
  convergence_tol = 0.001,
  convergence_window = 3L,
  trust_region_size = 0.1,
  trust_region_rho_lb = 1e-04,
  trust_region_rho_ub = 0.99,
  trust_region_scalar = 5,
  max_trust_iterations = 100L,
  minimum_likelihood_increase = 1e-06,
  reset_trust_region = FALSE,
  intercept = 1,
  link_fn_denominator = 6e+06,
  diagonal_method = "xdiag",
  n_samples = 100L,
  seed = NULL,
  num_jackknife_blocks = 100L,
  max_chisq_threshold = NULL,
  use_surrogate_markers = FALSE,
  surrogate_maps = NULL,
  surrogate_markers_path = NULL,
  block_names = NULL,
  score_test_hdf5 = NULL,
  score_test_trait_name = "trait",
  score_test_variant_data = NULL,
  score_test_jackknife_blocks = NULL,
  score_test_diagonal_method = diagonal_method,
  score_test_n_samples = 200L,
  score_test_write_hessian = FALSE,
  score_test_project_annotations = TRUE,
  score_test_overwrite = FALSE
)
}
\arguments{
\item{ldgms}{An \code{ldgm_precision}/sparse precision block, a list of blocks,
or an \code{ldgm_reml_inputs} object returned by \code{\link[=ldgm_prepare_reml_inputs]{ldgm_prepare_reml_inputs()}}.}

\item{z}{Numeric Z-score vector or list of vectors, one per block. Ignored
when \code{ldgms} is an \code{ldgm_reml_inputs} object.}

\item{annotations}{Numeric annotation matrix or list of matrices, one per
block. Ignored when \code{ldgms} is an \code{ldgm_reml_inputs} object.}

\item{params}{Optional starting parameter vector. Defaults to zeros.}

\item{sample_size}{Positive GWAS sample size. When \code{ldgms} is an
\code{ldgm_reml_inputs} object, defaults to the prepared \code{sample_size}.}

\item{annotation_names}{Optional names for annotation parameters. Defaults to
annotation matrix column names or \code{annot1}, \code{annot2}, ...}

\item{num_iterations}{Maximum number of optimization iterations.}

\item{convergence_tol}{Per-iteration convergence tolerance used with
\code{convergence_window}, matching GraphLD's graphREML stopping rule.}

\item{convergence_window}{Number of recent likelihood values used in the
convergence check.}

\item{trust_region_size}{Initial trust-region lambda.}

\item{trust_region_rho_lb}{Lower acceptance bound for the actual-versus-
predicted likelihood ratio.}

\item{trust_region_rho_ub}{Upper bound above which the trust-region lambda is
decreased for the next iteration.}

\item{trust_region_scalar}{Multiplicative factor used when expanding or
shrinking the trust region.}

\item{max_trust_iterations}{Maximum number of trust-region retries per outer
optimization step.}

\item{minimum_likelihood_increase}{Minimum predicted increase below which a
non-negative trust-region step is accepted without further lambda growth.}

\item{reset_trust_region}{If \code{TRUE}, reset the trust-region lambda to
\code{trust_region_size} at every outer iteration.}

\item{intercept, link_fn_denominator, diagonal_method, n_samples, seed}{Passed to
\code{ldgm_reml_block()}.}

\item{num_jackknife_blocks}{Maximum number of grouped block jackknife
replicates used for parameter, heritability, and enrichment standard errors.}

\item{max_chisq_threshold}{Optional maximum block chi-square threshold. Blocks
whose maximum \code{z^2} exceeds this value are excluded, matching GraphLD's
high-chi-square block guard.}

\item{use_surrogate_markers}{If \code{TRUE}, replace missing per-variant Z scores
using \code{\link[=ldgm_reml_surrogate_markers]{ldgm_reml_surrogate_markers()}} before fitting.}

\item{surrogate_maps}{Optional one-based surrogate index map, or list of maps,
used when \code{use_surrogate_markers = TRUE}.}

\item{surrogate_markers_path}{Optional GraphLD-style HDF5 file containing one
zero-based surrogate-map dataset per block. Cannot be supplied together
with \code{surrogate_maps}.}

\item{block_names}{Optional block names used to read \code{surrogate_markers_path}.
Defaults to names of \code{ldgms}, then \code{block1}, \code{block2}, ...}

\item{score_test_hdf5}{Optional HDF5 path. When supplied, final per-variant
score-test gradients are written with \code{\link[=ldgm_write_score_test_hdf5]{ldgm_write_score_test_hdf5()}}.}

\item{score_test_trait_name}{Trait group name to use under \verb{/traits} when
\code{score_test_hdf5} is supplied.}

\item{score_test_variant_data}{Data frame, or list of per-block data frames,
with \code{CHR}, \code{POS}, and \code{RSID}/\code{SNP} columns for HDF5 row data. When
\code{ldgms} is an \code{ldgm_reml_inputs} object, this defaults to the prepared
metadata-backed active rows.}

\item{score_test_jackknife_blocks}{Optional jackknife assignments for the HDF5
row data.}

\item{score_test_diagonal_method, score_test_n_samples}{Inverse-diagonal
estimator and probe count used for final per-variant score gradients and
optional Hessian/correction vectors.}

\item{score_test_write_hessian}{If \code{TRUE}, also compute and write the
GraphLD-style per-variant Hessian/correction vector to the score-test HDF5
trait group.}

\item{score_test_project_annotations}{If \code{TRUE}, project annotation columns
out of final per-variant score-test gradients before writing, matching
GraphLD's score-test path.}

\item{score_test_overwrite}{If \code{TRUE}, replace an existing HDF5 file.}
}
\value{
A list containing estimated \code{parameters}, jackknife standard errors
and log10 p-values, \code{heritability}, \code{enrichment}, \code{likelihood_history},
convergence diagnostics, \code{score_test_hdf5} write metadata when requested,
and final block derivatives.
}
\description{
Initial serial scheduler for GraphLD-style graphREML over one or more LDGM
blocks. Each iteration sums block likelihoods, gradients, and
average-information Hessians from \code{ldgm_reml_block()}, proposes a Newton step,
and uses step-halving to require non-decreasing likelihood.
}
\details{
This function is intended as the first R-native workflow surface and
conformance target for graphREML kernels. It includes initial GraphLD-style
pseudo-jackknife standard errors, optional score-test HDF5 output, and an
initial serial surrogate-marker path for missing Z scores. It does not yet
implement GraphLD's multiprocessing manager.
}
