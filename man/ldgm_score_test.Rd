% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score-test.R
\name{ldgm_score_test}
\alias{ldgm_score_test}
\title{Run GraphLD-Style Annotation Score Tests}
\usage{
ldgm_score_test(
  gradient,
  annotations,
  jackknife_blocks = NULL,
  annotation_names = NULL
)
}
\arguments{
\item{gradient}{Numeric per-variant score/gradient vector.}

\item{annotations}{Numeric annotation matrix/data frame with one row per
\code{gradient} entry and one column per annotation to test.}

\item{jackknife_blocks}{Optional integer block assignment vector. If omitted,
all variants are assigned to block zero; standard errors and Z statistics
are then \code{NA} because at least two blocks are required.}

\item{annotation_names}{Optional names for annotation columns. Defaults to
column names or \code{annot1}, \code{annot2}, ...}
}
\value{
A list with \code{results}, \code{block_scores}, and \code{jackknife_scores}.
\code{results} is a data frame with annotation name, total score, jackknife
standard error, Z statistic, and two-sided log10 p-value.
}
\description{
Computes the block jackknife score-test statistic used by GraphLD's
\code{score_test.py} path. The current upstream statistic uses per-variant
gradients, annotation values to test, and jackknife block assignments; any
HDF5 \code{hessian}/correction vector is preserved by the I/O helpers but is not
part of this staged statistic.
}
