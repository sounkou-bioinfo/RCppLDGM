% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score-test.R
\name{ldgm_score_test_hdf5}
\alias{ldgm_score_test_hdf5}
\title{Run Annotation Score Tests from a GraphLD-Style HDF5 File}
\usage{
ldgm_score_test_hdf5(
  file,
  trait_name,
  annotations,
  by = "RSID",
  annotation_cols = NULL,
  gene_table = NULL,
  nearest_weights = NULL
)
}
\arguments{
\item{file}{HDF5 path written by \code{\link[=ldgm_write_score_test_hdf5]{ldgm_write_score_test_hdf5()}} or compatible
GraphLD tooling.}

\item{trait_name}{Trait group name under \verb{/traits}.}

\item{annotations}{Numeric matrix or data frame of annotations to test. Data
frames can be matched to HDF5 row data by \code{by}; matrices are interpreted in
HDF5 row order.}

\item{by}{Optional key column used to match annotation data frames to HDF5 row
data, usually \code{RSID}. Use \code{NULL} to require row-order alignment. When
\code{annotations} is a named gene-set list and the HDF5 file is gene-level,
the default falls back to \code{gene_id} or \code{gene_name} as needed.}

\item{annotation_cols}{Optional annotation columns or gene-set names to test
when \code{annotations} is a data frame or named gene-set list. Defaults to all
annotation columns except \code{by}.}

\item{gene_table}{Optional gene table used when \code{annotations} is a named
gene-set list and the HDF5 file is variant-level. It is ignored for matrix
and data-frame annotations.}

\item{nearest_weights}{Optional nearest-gene weights used when \code{annotations}
is a named gene-set list and the HDF5 file is variant-level.}
}
\value{
A list like \code{ldgm_score_test()}, with an additional \code{variant_data}
component containing matched HDF5 rows.
}
\description{
Reads a trait from \code{\link[=ldgm_read_score_test_hdf5]{ldgm_read_score_test_hdf5()}}, aligns variant annotations,
and runs \code{ldgm_score_test()}. This is a small R-native counterpart to the
variant-annotation branch of GraphLD's \code{score_test.py} command.
}
