% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interfaces.R
\name{ldgm_score_test_variant_data_provider}
\alias{ldgm_score_test_variant_data_provider}
\title{Create a Callback-Based Variant Score-Test Row-Data Provider}
\usage{
ldgm_score_test_variant_data_provider(
  frame,
  required_cols = c("CHR", "POS", "RSID")
)
}
\arguments{
\item{frame}{Function expected to return a data frame.}

\item{required_cols}{Default row-data columns returned when callers do not
request additional columns. Must include \code{CHR}, \code{POS}, and \code{RSID} or
\code{SNP}.}
}
\value{
An object implementing \link{LdgmScoreTestVariantData}.
}
\description{
Builds an object implementing \link{LdgmScoreTestVariantData} from a callback
rather than an eagerly materialized data frame. This is intended for query-
backed or out-of-memory row tables, for example DuckDB relations. The
callback is called as \verb{frame(required_cols = <chr>)} and should return a data
frame containing at least \code{CHR}, \code{POS}, and \code{RSID} or \code{SNP}; callers can ask
for additional columns such as \code{jackknife_blocks}.
}
