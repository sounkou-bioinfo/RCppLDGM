% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interfaces.R
\name{ldgm_summary_stats_provider}
\alias{ldgm_summary_stats_provider}
\title{Create a Callback-Based Summary-Statistics Provider}
\usage{
ldgm_summary_stats_provider(
  frame,
  required_cols = c("SNP", "Z"),
  partition = NULL
)
}
\arguments{
\item{frame}{Function called as \verb{frame(required_cols = <chr>)} and expected to
return a data frame.}

\item{required_cols}{Default columns required when callers do not request a
narrower projection.}

\item{partition}{Optional function called as
\verb{partition(metadata, chrom_col = NULL, pos_col = NULL, required_cols = <chr>)}
and expected to return a list of data frames, one per metadata row.}
}
\value{
An object implementing \link{LdgmSummaryStats}.
}
\description{
Builds an object implementing \link{LdgmSummaryStats} from callback functions
rather than an eagerly materialized data frame. This is intended for query-
backed or out-of-memory tables, for example DuckDB relations. The \code{frame}
callback should return a data frame when asked for projected columns; the
optional \code{partition} callback can partition by LDGM metadata blocks without
scanning the full table into R first.
}
