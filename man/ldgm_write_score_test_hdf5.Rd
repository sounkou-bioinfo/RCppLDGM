% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdf5.R
\name{ldgm_write_score_test_hdf5}
\alias{ldgm_write_score_test_hdf5}
\title{Write GraphLD-Style Score-Test HDF5 Output}
\usage{
ldgm_write_score_test_hdf5(
  file,
  variant_data,
  gradient,
  trait_name = "trait",
  jackknife_blocks = NULL,
  hessian = NULL,
  parameters = NULL,
  jackknife_parameters = NULL,
  overwrite = FALSE,
  compression = c("lzf", "gzip", "none"),
  chunk_size = 1000L,
  source = paste0("RcppLDGM v", utils::packageVersion("RcppLDGM"))
)
}
\arguments{
\item{file}{Output HDF5 path. Existing files are updated by appending a new
trait unless \code{overwrite = TRUE}.}

\item{variant_data}{Data frame with \code{CHR}, \code{POS}, and either \code{RSID} or \code{SNP}.}

\item{gradient}{Numeric variant score/gradient vector, one value per row of
\code{variant_data}.}

\item{trait_name}{HDF5 trait group name under \verb{/traits}. Must not contain \code{/}.}

\item{jackknife_blocks}{Optional integer jackknife block assignment vector. If
omitted, \code{variant_data$jackknife_blocks} is used when present, otherwise all
variants are assigned to block zero.}

\item{hessian}{Optional numeric variant Hessian/correction vector, one value
per row of \code{variant_data}, stored as \verb{/traits/<trait_name>/hessian}.}

\item{parameters}{Optional numeric fitted parameter vector stored under the
trait's \code{parameters} group for GraphLD score-test I/O compatibility.}

\item{jackknife_parameters}{Optional numeric matrix with one row per
jackknife replicate and one column per \code{parameters} entry. Required when
\code{parameters} is supplied.}

\item{overwrite}{If \code{TRUE}, replace any existing file before writing.}

\item{compression}{One of \code{"lzf"}, \code{"gzip"}, or \code{"none"}. \code{"lzf"} matches
GraphLD's current score-test output.}

\item{chunk_size}{Positive HDF5 chunk length.}

\item{source}{Source string stored as a root HDF5 attribute.}
}
\value{
Invisibly, a list describing the written file and trait.
}
\description{
Writes the HDF5 layout used by GraphLD's graphREML score-test path: root
metadata attributes, \verb{/row_data/\{CHR,POS,RSID,jackknife_blocks\}}, \verb{/groups},
\verb{/traits/<trait_name>/gradient}, and optionally
\verb{/traits/<trait_name>/hessian} plus
\verb{/traits/<trait_name>/parameters/\{parameters,jackknife_parameters\}}. Native
HDF5 support is linked through the CRAN \code{hdf5lib} package, including its
bundled LZF/gzip filters.
}
