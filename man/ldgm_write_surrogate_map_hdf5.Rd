% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdf5.R
\name{ldgm_write_surrogate_map_hdf5}
\alias{ldgm_write_surrogate_map_hdf5}
\title{Write a GraphLD-Style Surrogate-Marker HDF5 Map}
\usage{
ldgm_write_surrogate_map_hdf5(
  file,
  block_name,
  surrogate_map,
  overwrite = FALSE,
  file_index_base = c("zero", "one"),
  compression = c("lzf", "gzip", "none"),
  chunk_size = 1000L
)
}
\arguments{
\item{file}{Output HDF5 path.}

\item{block_name}{Dataset name for the LD block. Must not contain \code{/}.}

\item{surrogate_map}{Integer vector of one-based surrogate precision indices,
one entry per active precision node. \code{NA} entries are allowed and stored as
\code{-1} sentinels.}

\item{overwrite}{If \code{TRUE}, replace any existing file before writing.}

\item{file_index_base}{Index convention used inside the HDF5 dataset. The
default \code{"zero"} matches GraphLD/Python; \code{"one"} is mostly for R-only
round trips.}

\item{compression}{One of \code{"lzf"}, \code{"gzip"}, or \code{"none"}.}

\item{chunk_size}{Positive HDF5 chunk length.}
}
\value{
Invisibly, a list describing the written dataset.
}
\description{
Writes one per-block surrogate-marker dataset at the HDF5 root, matching the
layout read by GraphLD's \code{surrogate_markers_path} option. R-facing surrogate
indices are one-based; by default they are stored as upstream GraphLD-style
zero-based integer indices. \code{NA} entries are stored as \code{-1} sentinels and read
back as \code{NA}.
}
