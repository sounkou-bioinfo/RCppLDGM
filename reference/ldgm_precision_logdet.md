# Log Determinant of an LDGM Precision Matrix

Computes `log(det(precision))` using R `Matrix` sparse determinant
methods.

## Usage

``` r
ldgm_precision_logdet(precision)
```

## Arguments

- precision:

  Sparse symmetric positive-definite precision matrix.

## Value

A single numeric log determinant.
