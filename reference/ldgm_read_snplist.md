# Read an LDGM SNP List File

Reads a comma-separated `.snplist` file as a data frame while preserving
column names used by `ldgm` and GraphLD.

## Usage

``` r
ldgm_read_snplist(path)
```

## Arguments

- path:

  Path to the snplist file.

## Value

A data frame.
